let myString: string;

myString = 'This is a string';

const myConstantString: string = 'My constant string';

let aString: string;
let aNumber: number;
let aBoolean: boolean;
let anArray: Array<string>;
let anything: any; //can be used if we don't know the type.

let anotherString = 'A string without the declared type.'
